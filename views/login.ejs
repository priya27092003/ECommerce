<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 flex items-center justify-center">
        <div class="w-full max-w-md">
            <h3 class="text-3xl mb-5 text-center">Login</h3>
            <form action="/users/login" method="post" class="space-y-4">
                <div>
                    <label for="email" class="block mb-1">Email</label>
                    <input id="email" class="input-field w-full" type="email" placeholder="Your Email" name="email" required>
                </div>
                <div>
                    <label for="password" class="block mb-1">Password</label>
                    <input id="password" class="input-field w-full" type="password" placeholder="Your Password" name="password" required>
                </div>
                <input type="submit" value="Login" class="w-full px-5 py-2 rounded-md bg-blue-500 hover:bg-blue-600 transition">
            </form>            
            <p class="text-center text-sm mt-4">
                Don't have an account? 
                <a href="/register" class="text-blue-500 hover:underline">Sign Up</a>
            </p>
        </div>
    </div>

    <style>
        .input-field {
            @apply px-4 py-3 rounded-md bg-zinc-800 text-white border-2 border-zinc-600 outline-none;
        }
        .input-field:focus {
            @apply border-blue-500;
        }
    </style>
</body>
</html>
